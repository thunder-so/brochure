version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npx pnpm i --store=node_modules/.pnpm-store
    build:
      commands:
        -  npx pnpm run build
  artifacts:
    baseDirectory: docs/.vitepress/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*